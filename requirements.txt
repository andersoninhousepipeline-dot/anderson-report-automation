# PGT-A Report Generator - Requirements
# =====================================
# Install with: pip install -r requirements.txt

# Core GUI Framework
PyQt6>=6.6.0
PyQt6-Qt6>=6.6.0

# PDF Generation
reportlab>=4.0.0
PyPDF2>=3.0.0
pdfplumber>=0.10.0

# Word Document Generation
python-docx>=1.0.0

# Data Processing
pandas>=2.0.0
openpyxl>=3.1.0
pyarrow>=14.0.0

# Image Processing
Pillow>=10.0.0
numpy>=1.24.0

# HTTP requests
requests>=2.31.0

# OCR for TRF Verification (OPTIONAL)
# Note: EasyOCR requires PyTorch which can have issues on some Windows systems
# If PyTorch fails to load, the app will still work but TRF verification is disabled
# To fix PyTorch DLL errors on Windows, install Visual C++ Redistributable:
# https://aka.ms/vs/17/release/vc_redist.x64.exe
easyocr>=1.7.0
